name: Deploy to Railway

on:
  push:
    branches: [ main ]

jobs:
  deploy-auth-service:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build auth-service
        run: |
          cd auth-service
          mvn clean package -DskipTests

      - name: Deploy auth-service
        shell: bash
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          RAILWAY_PROJECT_ID: ${{ secrets.RAILWAY_PROJECT_ID }}
        run: |
          curl -fsSL https://railway.com/install.sh | bash
          cd auth-service
          ~/.railway/bin/railway up --service auth-service

  deploy-apunte-service:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build apunte-service
        run: |
          cd apunte-service
          mvn clean package -DskipTests

      - name: Deploy apunte-service
        shell: bash
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          RAILWAY_PROJECT_ID: ${{ secrets.RAILWAY_PROJECT_ID }}
        run: |
          curl -fsSL https://railway.com/install.sh | bash
          cd apunte-service
          ~/.railway/bin/railway up --service apunte-service

  deploy-materia-service:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build materia-service
        run: |
          cd materia-service
          mvn clean package -DskipTests

      - name: Deploy materia-service
        shell: bash
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          RAILWAY_PROJECT_ID: ${{ secrets.RAILWAY_PROJECT_ID }}
        run: |
          curl -fsSL https://railway.com/install.sh | bash
          cd materia-service
          ~/.railway/bin/railway up --service materia-service

  deploy-api-gateway:
    runs-on: ubuntu-latest
    needs: [deploy-auth-service, deploy-apunte-service, deploy-materia-service]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build api-gateway
        run: |
          cd api-gateway
          mvn clean package -DskipTests

      - name: Deploy api-gateway
        shell: bash
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
          RAILWAY_PROJECT_ID: ${{ secrets.RAILWAY_PROJECT_ID }}
        run: |
          curl -fsSL https://railway.com/install.sh | bash
          cd api-gateway
          ~/.railway/bin/railway up --service api-gateway

#PRUEBA