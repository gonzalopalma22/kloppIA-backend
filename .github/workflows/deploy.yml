name: Deploy to Railway

on:
  push:
    branches: [ main ]

jobs:
  deploy-auth-service:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy auth-service
        shell: bash
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          curl -fsSL https://railway.com/install.sh | bash
          source "$HOME/.railway/env"
          cd auth-service
          railway up --service auth-service --project ${{ secrets.RAILWAY_PROJECT_ID }} --environment production

  deploy-apunte-service:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy apunte-service
        shell: bash
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          curl -fsSL https://railway.com/install.sh | bash
          source "$HOME/.railway/env"
          cd apunte-service
          railway up --service apunte-service --project ${{ secrets.RAILWAY_PROJECT_ID }} --environment production

  deploy-materia-service:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy materia-service
        shell: bash
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          curl -fsSL https://railway.com/install.sh | bash
          source "$HOME/.railway/env"
          cd materia-service
          railway up --service materia-service --project ${{ secrets.RAILWAY_PROJECT_ID }} --environment production

  deploy-api-gateway:
    runs-on: ubuntu-latest
    needs: [deploy-auth-service, deploy-apunte-service, deploy-materia-service]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy api-gateway
        shell: bash
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          curl -fsSL https://railway.com/install.sh | bash
          source "$HOME/.railway/env"
          cd api-gateway
          railway up --service api-gateway --project ${{ secrets.RAILWAY_PROJECT_ID }} --environment production
